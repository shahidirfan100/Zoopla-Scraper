{
  "title": "Zoopla Property Scraper",
  "description": "Stealthy Zoopla scraper using PlaywrightCrawler with Camoufox for Cloudflare bypass. Extracts property listings with prices, addresses, beds, baths, and details.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Search URL",
      "type": "string",
      "description": "Zoopla search URL to scrape. Example: https://www.zoopla.co.uk/for-sale/property/london/",
      "editor": "textfield",
      "prefill": "https://www.zoopla.co.uk/for-sale/property/london/?q=London&search_source=home&recent_search=true"
    },
    "startUrls": {
      "title": "Multiple search URLs",
      "type": "array",
      "description": "Optional list of multiple Zoopla search URLs to crawl.",
      "editor": "stringList"
    },
    "results_wanted": {
      "title": "Maximum listings",
      "type": "integer",
      "description": "Maximum number of property listings to collect. Pages are automatically calculated (28 listings per page).",
      "minimum": 1,
      "maximum": 1000,
      "default": 50,
      "prefill": 50,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "REQUIRED: Use Apify RESIDENTIAL proxies with UK country code for best results.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ],
        "countryCode": "GB"
      },
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ],
        "countryCode": "GB"
      }
    }
  },
  "required": [
    "proxyConfiguration"
  ]
}