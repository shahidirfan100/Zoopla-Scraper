{
  "title": "Zoopla Property Scraper",
  "description": "JSON-first Zoopla scraper with HTML and sitemap fallbacks. Supports pagination, detail extraction, and proxy configuration.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Primary Zoopla search URL",
      "type": "string",
      "description": "Single Zoopla search URL (for-sale).",
      "editor": "textfield",
      "example": "https://www.zoopla.co.uk/for-sale/property/london/?q=London&search_source=home&recent_search=true",
      "default": "https://www.zoopla.co.uk/for-sale/property/london/?q=London&search_source=home&recent_search=true",
      "prefill": "https://www.zoopla.co.uk/for-sale/property/london/?q=London&search_source=home&recent_search=true"
    },
    "startUrls": {
      "title": "Multiple search URLs",
      "type": "array",
      "description": "Optional list of Zoopla search URLs to crawl.",
      "editor": "stringList",
      "example": [
        "https://www.zoopla.co.uk/for-sale/property/london/?q=London&search_source=home&recent_search=true"
      ]
    },
    "results_wanted": {
      "title": "Maximum listings to extract",
      "type": "integer",
      "description": "Maximum number of property listings to collect (1-1000).",
      "minimum": 1,
      "maximum": 1000,
      "default": 50,
      "prefill": 50,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum pages",
      "type": "integer",
      "description": "Maximum number of search pages to process per URL.",
      "minimum": 1,
      "maximum": 20,
      "editor": "number",
      "default": 3,
      "prefill": 3
    },
    "collectDetails": {
      "title": "Collect full property details",
      "type": "boolean",
      "description": "Visit each property detail page to extract descriptions, images, and structured data.",
      "editor": "checkbox",
      "default": true,
      "prefill": true
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Recommended: use Apify Proxy for reliability.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      },
      "prefill": {
        "useApifyProxy": true,
        "apifyProxyGroups": [
          "RESIDENTIAL"
        ]
      }
    }
  }
}
